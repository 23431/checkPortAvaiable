{
  "name": "check-avaiable-port",
  "version": "1.0.1",
  "description": "check port avaiable",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "typings": "dist/types/index.d.ts",
  "scripts": {
    "test": "ts-node ./src/test.ts",
    "build:cjs": "tsc -p .",
    "build:esm": "tsc -p . --module esnext --outDir dist/esm",
    "build:types": "tsc -p . -d --emitDeclarationOnly --outDir dist/types",
    "clean": "rimraf dist",
    "build": "yarn clean && run-p build:*"
  },
  "author": "xhl23431@gmail.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/xhl23431/check-avaiable-port.git"
  },
  "license": "ISC",
  "keywords": [
    "node",
    "ts",
    "port"
  ],
  "devDependencies": {
    "@types/node": "^20.12.7",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
